version: 2
jobs:
  test:
    machine:
      image: circleci/classic:latest

    steps:
      - checkout
      - run:
          name: run tests
          command: docker-compose run --rm packetmultiplexer make test

  deploy:
    machine:
      image: circleci/classic:latest

    steps:
      - checkout
      - run:
          name: compile distributable binaries
          command: docker-compose run --rm packetmultiplexer make dist
      - run:
          name: upload binaries to s3
          command: aws sync s3 dist/upload s3://builds.loraserver.io/lora-packet-multiplexer

workflows:
  version: 2
  test-and-deploy:
    jobs:
      - test:
          filters:
            tags:
              only: /.*/
      - deploy:
          requires:
            - test
          filters:
              tags:
                only: /^v.*/
              branches:
                ignore: /.*/

